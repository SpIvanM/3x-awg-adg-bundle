## Исходный промпт пользоваеля:

разбей единый скрипт на несколько скриптов, чтобы можно было их запускать отдельно.
Единый скрипт запускает отдельные скрипты как аркестратор:
Скрипты:

- Настройка VPS: обновление, безопасность, файл подкачки, mc
- 3x
- AmneziaWG
- AdguardHome
- (port forwarding - уже есть )
- Печать всех конфигураций

Зафиксируй модульность в принципах

## Улучшенный промпт, развертутое ТЗ:

1. Роль: Ты - Senior DevOps Engineer и архитектор инфраструктурных скриптов, обладаешь глубоким пониманием bash, архитектуры deployment-скриптов и TDD.
2. Суть: Текущий монолитный bash-скрипт `setup.sh` склеивается из нескольких source-модулей с помощью `tools/build-setup.ps1`. Вместо этого пользователь хочет, чтобы скрипт стал легковесным оркестратором, который просто загружает (или вызывает) уже самостоятельные скрипты-модули: настройки VPS, 3x, AmneziaWG, AdguardHome и вывода конфигураций. Это позволит запускать каждый этап отдельно для отладки или частичного развертывания.
3. Цель: Реорганизовать `src/setup` в набор самостоятельных исполняемых скриптов. Сделать `setup.sh` оркестратором, который их вызывает. Обновить `AGENTS.md` (принципы проекта) для отражения новой модульной архитектуры.
4. Шаги:
   - [X] З1: Переписать `AGENTS.md` - зафиксировать модульность в принципах
     КП1: Добавлен раздел или обновлено описание, что `setup.sh` является только оркестратором, а логика разбита на самостоятельные запускаемые скрипты.
     Контекст: `AGENTS.md`
     Формат: Обновленный markdown файл.
     Ограничения: Не менять остальную логику проекта в `AGENTS.md`.
   - [X] З2: Подготовить скрипт настройки VPS
     КП1: Создан новый скрипт (например, `setup-vps.sh`), который выполняет обновление, настройку безопасности, создание файла подкачки и установку mc.
     Контекст: `src/setup/20-system.sh`
     Формат: Новый bash-скрипт.
     Ограничения: Не удалять и не изменять существующие скрипты! Создавать только новые файлы.
   - [X] З3: Подготовить скрипт настройки 3x
     КП1: Создан новый скрипт (например, `setup-3x.sh`), настраивающий 3x-ui.
     Контекст: `src/setup/30-xray.sh`, `src/setup/13-3x-helpers.sh`
     Формат: Новый bash-скрипт.
     Ограничения: Не удалять и не изменять существующие скрипты!
   - [X] З4: Подготовить скрипт настройки AmneziaWG
     КП1: Создан новый скрипт (например, `setup-awg.sh`), настраивающий AmneziaWG.
     Контекст: `src/setup/40-awg.sh`, `src/setup/11-awg-helpers.sh`
     Формат: Новый bash-скрипт.
     Ограничения: Не удалять и не изменять существующие скрипты!
   - [X] З5: Подготовить скрипт настройки AdguardHome
     КП1: Создан новый скрипт (например, `setup-agh.sh`), настраивающий AdguardHome.
     Контекст: `src/setup/50-adguard.sh`, `src/setup/12-agh-helpers.sh`
     Формат: Новый bash-скрипт.
     Ограничения: Не удалять и не изменять существующие скрипты!
   - [X] З6: Подготовить скрипт печати всех конфигураций
     КП1: Создан новый скрипт (например, `setup-output.sh`), печатающий итоговые конфигурации.
     Контекст: `src/setup/70-output.sh`
     Формат: Новый bash-скрипт.
     Ограничения: Не удалять и не изменять существующие скрипты!
   - [X] З7: Создать оркестратор `setup-orchestrator.sh`
     КП1: Новый оркестратор принимает аргументы (`--mode`, `-r`), устанавливает глобальные переменные и запускает отдельные созданные скрипты-модули.
     КП2: Оркестратор должен скачивать модули во временную директорию перед исполнением.
     Контекст: `tools/build-setup.ps1`, `src/setup/00-bootstrap.sh`
     Формат: Новый bash-скрипт.
     Ограничения: Не изменять оригинальный `setup.sh` до отладки. Сохранить совместимость с `curl | bash` установкой.
